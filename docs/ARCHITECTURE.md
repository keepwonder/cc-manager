# cc-manager 架构说明

## 为什么有两个文件？

bin 目录下有两个文件，这是一个**双层架构设计**：

```
bin/
├── cc-manager          # 包装脚本（前端）
└── cc-manager-bin      # 核心程序（后端）
```

## 各文件的功能

### 1. cc-manager-bin (核心程序)

**类型**: 可执行 Bash 脚本
**作用**: 真正干活的程序

```bash
#!/usr/bin/env bash
# 这是原始的 cc-manager 程序
# 包含所有核心逻辑：
# - 加载配置
# - 解析命令
# - 执行操作（switch, list, status, etc.）
# - 管理提供商
```

**功能**:
- ✅ 所有命令的实际执行
- ✅ 配置文件管理
- ✅ 提供商切换逻辑
- ✅ 历史记录管理
- ✅ 生成环境变量导出命令（当 CC_EXPORT_MODE=1 时）

**可以独立使用**:
```bash
./bin/cc-manager-bin list
./bin/cc-manager-bin status
```

### 2. cc-manager (包装脚本)

**类型**: 包装器/前端脚本
**作用**: 用户体验增强

```bash
#!/usr/bin/env bash
# 这是一个智能包装器
# 检测 shell 集成状态
# 提供友好的用户提示
# 转发命令到 cc-manager-bin
```

**功能**:
- ✅ 检测用户是否启用了 shell 集成
- ✅ 当用户使用 `switch`/`back` 且未启用集成时，显示帮助信息
- ✅ 转发所有命令到 `cc-manager-bin`
- ✅ 提供友好的用户体验

**执行流程**:
```bash
用户输入: cc-manager switch deepseek
    ↓
1. cc-manager 脚本运行
    ↓
2. 检测: 这是 switch 命令吗？
    ↓ 是
3. 检测: 启用了 shell 集成吗？
    ↓ 没有
4. 显示帮助信息 ⚠️
    ↓
5. 继续执行: exec cc-manager-bin switch deepseek
    ↓
6. ❌ 环境变量只在子进程中设置，不持久化
```

### 3. shell-integration.sh 中的 cc-manager 函数

**类型**: Shell 函数
**作用**: 真正的环境变量注入

当你 source shell-integration.sh 后：

```bash
# 定义了一个 cc-manager 函数
cc-manager() {
    local command="$1"

    case "$command" in
        switch|sw)
            # 特殊处理：获取导出命令并 eval
            local output=$(CC_EXPORT_MODE=1 cc-manager-bin "$@")
            echo "$output" | grep -v "^export"
            eval "$(echo "$output" | grep '^export\|^unset')"
            ;;
        *)
            # 其他命令直接转发
            cc-manager-bin "$@"
            ;;
    esac
}
```

**功能**:
- ✅ **覆盖** bin/cc-manager 命令
- ✅ 拦截所有 cc-manager 命令
- ✅ 对 switch/back 特殊处理（注入环境变量）
- ✅ 其他命令直接透传

## 三层架构图

### 未启用 Shell 集成

```
┌─────────────────────────────────────────────┐
│ 用户输入: cc-manager switch deepseek        │
└─────────────────┬───────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────┐
│ bin/cc-manager (包装脚本)                   │
│ - 检测到 switch 命令                        │
│ - 检测到未启用 shell 集成                   │
│ - 显示帮助信息 ⚠️                           │
└─────────────────┬───────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────┐
│ bin/cc-manager-bin (核心程序)              │
│ - 执行 switch 逻辑                          │
│ - 在子进程中设置环境变量                    │
│ - 返回成功信息                              │
└─────────────────┬───────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────┐
│ 结果: ❌ 环境变量不持久化                   │
│ 原因: 在子进程中设置，父 shell 无法访问    │
└─────────────────────────────────────────────┘
```

### 启用 Shell 集成后

```
┌─────────────────────────────────────────────┐
│ 用户输入: cc-manager switch deepseek        │
└─────────────────┬───────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────┐
│ cc-manager() 函数 (shell-integration.sh)   │
│ - 拦截命令                                  │
│ - 识别 switch 命令                          │
│ - 需要特殊处理 ✓                            │
└─────────────────┬───────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────┐
│ 调用: CC_EXPORT_MODE=1 cc-manager-bin ...  │
│                                             │
│ bin/cc-manager-bin (核心程序)              │
│ - 执行 switch 逻辑                          │
│ - 检测到 CC_EXPORT_MODE=1                  │
│ - 输出 export 命令                          │
│   export ANTHROPIC_BASE_URL="..."          │
│   export ANTHROPIC_AUTH_TOKEN="..."        │
└─────────────────┬───────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────┐
│ cc-manager() 函数捕获输出                   │
│ - 提取 export 命令                          │
│ - 在当前 shell 执行: eval "export ..."     │
└─────────────────┬───────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────┐
│ 结果: ✅ 环境变量持久化                     │
│ 原因: 在当前 shell 中设置（通过 eval）     │
└─────────────────────────────────────────────┘
```

## 为什么要这样设计？

### 问题：环境变量不能跨进程传递

Unix/Linux 的基本限制：
```bash
# 子进程无法修改父进程的环境变量
$ bash -c 'export FOO=bar'
$ echo $FOO
                    # 空的！因为 FOO 只在子进程中
```

### 解决方案：Shell 函数 + eval

Shell 函数在**当前 shell** 中执行：
```bash
my_function() {
    export FOO=bar  # 在当前 shell 中设置
}

my_function
echo $FOO          # bar ✓
```

### 为什么不直接用 shell 函数？

**问题**：如果只提供 shell 函数，用户未 source 时会报错：

```bash
$ cc-manager list
bash: cc-manager: command not found  # ❌ 不友好
```

**解决**：提供可执行文件作为后备：

```bash
$ cc-manager list
# 即使未启用集成，命令也能执行 ✓
# 只是 switch/back 时会提示需要启用集成
```

## 类比理解

### 类比 1：餐厅服务

```
cc-manager        = 前台服务员（接待客人，提供帮助）
cc-manager-bin    = 后厨师傅（实际做菜）
shell function    = VIP 通道（直接沟通，无需排队）
```

- **普通用户**（未启用集成）：通过前台 → 后厨
- **VIP 用户**（启用集成）：VIP 通道 → 后厨 → 特殊服务

### 类比 2：快递系统

```
cc-manager        = 快递站点（接收包裹，提供指引）
cc-manager-bin    = 快递车（实际运输）
shell function    = 上门取件（直接服务）
```

- **未启用集成**：去站点寄快递
- **启用集成**：快递员上门取件（更方便）

## 文件大小对比

```bash
$ ls -lh bin/
-rwx--x--x  cc-manager      2.8K  # 包装脚本（简单逻辑 + 帮助信息）
-rwx--x--x  cc-manager-bin  2.2K  # 核心程序（实际逻辑）
```

## 命令执行路径

### 情况 1: 未启用 Shell 集成

```bash
which cc-manager
# /usr/local/bin/cc-manager (可执行文件)

type cc-manager
# cc-manager is /usr/local/bin/cc-manager (不是函数)

cc-manager switch deepseek
# 执行路径:
# /usr/local/bin/cc-manager → /usr/local/bin/cc-manager-bin
# 结果: 显示帮助，环境变量不持久化
```

### 情况 2: 启用 Shell 集成

```bash
source /usr/local/lib/cc-manager/shell-integration.sh

which cc-manager
# cc-manager: shell function  # 已被函数覆盖！

type cc-manager
# cc-manager is a function (不再是可执行文件)

cc-manager switch deepseek
# 执行路径:
# cc-manager (函数) → cc-manager-bin → eval
# 结果: 环境变量持久化 ✓
```

## 优势总结

### ✅ 用户友好
- 未启用集成时提供帮助信息
- 命令仍然可以执行（虽然效果不完全）

### ✅ 功能完整
- 启用集成后环境变量正确持久化
- 统一的命令接口

### ✅ 灵活性
- 可以选择是否启用集成
- 脚本可以直接调用 cc-manager-bin（不需要集成）

### ✅ 向后兼容
- 旧的调用方式仍然工作
- 新用户通过提示学会正确使用

## 实际演示

### 未启用集成

```bash
$ cc-manager switch deepseek
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️  IMPORTANT: Shell Integration Required
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

For 'switch' and 'back' commands to work properly...
[... 帮助信息 ...]

Continuing with command (environment variables will NOT persist)...

✓ Switched to deepseek
  BASE_URL: https://api.deepseek.com/anthropic

$ echo $ANTHROPIC_BASE_URL
                           # 空的！❌
```

### 启用集成后

```bash
$ source /usr/local/lib/cc-manager/shell-integration.sh

$ cc-manager switch deepseek
✓ Switched to deepseek
  BASE_URL: https://api.deepseek.com/anthropic

$ echo $ANTHROPIC_BASE_URL
https://api.deepseek.com/anthropic  # 有值！✓
```

## 总结

| 文件 | 类型 | 作用 | 何时执行 |
|------|------|------|---------|
| `cc-manager` | Wrapper | 提供帮助，转发命令 | 未启用集成时 |
| `cc-manager-bin` | 核心程序 | 实际执行逻辑 | 总是 |
| `cc-manager()` 函数 | Shell 函数 | 环境变量注入 | 启用集成后 |

**关键点**：
- 📦 `cc-manager-bin` 是核心（做实事）
- 🎯 `cc-manager` 是前端（提供帮助）
- 🔧 Shell 函数是魔法（环境变量持久化）

这个设计让工具既**友好**又**功能完整**！
